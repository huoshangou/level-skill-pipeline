# Build output
dist/

# Node
node_modules/
package-lock.json

# Pipeline runtime output (per-case generated artifacts)
src/outputs/
src/scoring/

# Per-case workspace — local only by default.
# Reference fixture: case_02_artmuseum is the only seed case kept in repo.
# (Earlier policy also tracked case_01; revised because per-case ir_filled.json
#  drifts every session and pollutes diffs.)
# Trailing slash omitted so it catches symlinks too.
src/test_cases/case_01_*
src/test_cases/case_03_*
src/test_cases/case_04_*
src/test_cases/case_05_*
src/test_cases/case_06_*

# Installer logs
*.log
levelagent_install_*.log
level-skill-pipeline_install_*.log

# Backups
*.bak.*
*.bak

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
